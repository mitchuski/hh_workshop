# SUITE_MAPPING.md — new processes → agentprivacy personas + skills (PROPOSALS)

*The hh_workshop build produced processes that did not exist in the suite
before. This doc maps each to a proposed skill (agentprivacy-skills-v5
naming) and a keeper persona from the City canon. **Proposals only** —
minting skills into the canonical repo, seating keepers, and any tome act
are the First Person's and the keystone's; nothing here writes outside this
directory.*

| # | new process (born here) | proposed skill | proposed keeper / persona | why that keeper |
|---|---|---|---|---|
| 1 | **the babblefish gate** — translation-fidelity harness: carry declarations per tongue, census over a frozen claim register, probe FORM drawn Fiat-Shamir, assayer blind to canon, translation-debt ↓ | `agentprivacy-babblefish-gate` | **Vagari 🌳** (Holon Hitchhikers, V31 — the Oasis Protocol) | the shop about wholes that travel and return carrying seeds; a triptych is exactly a whole carried across tongues, and this workshop is the shop's first real course |
| 2 | **generative-website derivation** — walk a repo's ledgers → typed KG (spellweb dialect) + self-contained site/wiki/graph; re-run = re-true; now also instance-generic in the framework (`tools/spellweb.mjs`) | `agentprivacy-generative-web` | **the Cartographer** (model visual runtime) | already keeps the live instruments on /model; this is the same law — projections derived from the source, never authored beside it |
| 3 | **the steward fold** — rotating human steward of a local model's training; consent-gated corpus at the pipe; pack-hash provenance chain, verified in public; handover rite | `agentprivacy-steward-fold` | **the Archivist 📚** (the Tower, Tome VIII) | the Tower keeps records that outlive their keepers; the steward log is exactly such a record — a chain of hands, replayable forever |
| 4 | **workshop law compiling** — invariants of a live practice written in the attested Lexon subset; relation claims with falsifiability twins; ABSENCE claims (extraction structurally impossible) | `agentprivacy-workshop-law` | seat **beside lexon_pvm** (its census is the natural home; the three laws here are ready-made draft expressions for its OT-8-era folds) | one spec-checker regime, one grammar — two homes would drift |
| 5 | **the local mage runtime** — pure-data seats + Ollama rt driver; all hashing code-side (the seat physically cannot see the salt); fail-closed offline replay; required-fields lesson (an optional field is a field a schema-forced model omits) | `agentprivacy-local-mage-rt` | **Soulbis ⚔️ ⊥ Soulbae 🧙** themselves (a HARNESS_PATHS pattern note, not a shop) | it is not a domain — it is how the pair sits down anywhere there is no cloud |
| 6 | **earned trust edges** — VRC edges minted only on verifiable events (mentor-assay, steward-fold, pair-swap, keystone-fold, seal-witness); no self-edges; consent-gated roster; deterministic hash-drawn pairing (the Gap's idiom, socially) | `agentprivacy-earned-edges` | the **🪢 VRC canon** (trust-graph layer), with the Game of 42 as its board | the 42's nodes are trust tasks; this is the edge-minting discipline they were waiting for |
| 7 | **the understanding-key auditor** — access ladders as honest AUDITORS (enumerable rungs + human door), never harnesses; graduated cookie-jar | `agentprivacy-understanding-key` | the **Gatehouse** (proof-of-reading kin) | same primitive — understanding-as-key — already gates the sealed rooms |

## Where each would land (when minted)

- skills → `agentprivacy_master/agentprivacy-skills/agentprivacy-skills-v5`
  (the canonical V5 repo), then the skill wiki via the wiki-sync flow;
- keeper seatings and any new shop/act → City canon through the keystone
  (tome discipline; this doc deliberately proposes NO count changes);
- the framework pattern notes (#5) → `dual-agent-harness/HARNESS_PATHS.md`
  prose, where instance #12's at-a-glance already carries the seed.

## The reflection already made

`tools/spellweb.mjs` now lives in the framework repo (uncommitted): the
instance-GENERIC autobuilder — config, frontier, runs, levers, claims,
killed levers, chronicles → graph.json + a self-contained web.html for any
of the twelve instances. hh_workshop keeps its richer instance-specific
walkers (claims register, triptychs, lexon triples, roster). Same dialect
top to bottom, so a future fold into spellweb-the-site's hand-curated data
is a straight emit — and each cohort can equally keep **their own**
spellweb, derived from their own instance, which may be the truer shape.
